name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Type Check, Lint & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

  deploy:
    name: Deploy to AWS S3 + CloudFront
    runs-on: ubuntu-latest
    needs: quality
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build for production
        run: npm run build
        env:
          NODE_ENV: production

      # Deployment steps below are ready but gated on infrastructure completion (issue #3).
      # Uncomment once S3 bucket, CloudFront distribution, and IAM role are provisioned.
      # Required secrets: AWS_DEPLOY_ROLE_ARN, AWS_S3_BUCKET, AWS_CLOUDFRONT_DISTRIBUTION_ID

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
      #     aws-region: us-east-1

      # - name: Sync to S3
      #   run: |
      #     aws s3 sync out/ s3://${{ secrets.AWS_S3_BUCKET }} \
      #       --delete \
      #       --cache-control "public, max-age=31536000, immutable" \
      #       --exclude "*.html" \
      #       --exclude "*.json"
      #     aws s3 sync out/ s3://${{ secrets.AWS_S3_BUCKET }} \
      #       --delete \
      #       --cache-control "no-cache, no-store, must-revalidate" \
      #       --include "*.html" \
      #       --include "*.json"

      # - name: Invalidate CloudFront cache
      #   run: |
      #     aws cloudfront create-invalidation \
      #       --distribution-id ${{ secrets.AWS_CLOUDFRONT_DISTRIBUTION_ID }} \
      #       --paths "/*"

      - name: Deploy placeholder
        run: echo "Infrastructure not yet provisioned (issue #3). Build artifact ready for deployment."
